import { spawn } from "child_process";

/**
 * This test is to test the builds of the project with various configurations and flags and see if they are successful
 */
describe("testBuilds", () => {
    it("should build successfully", () => {

    })
});