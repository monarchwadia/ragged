{
  "name": "ragged",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.js --outdir=./out --bundle --platform=node --target=node10.4",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "0.20.2"
  }
}